{
  "name": "ics-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "cd ics-backend && npm start",
    "db:seed": "cd ics-backend && npm run db:seed",
    "build": "cd ics-backend && npm install && npm run build"
  }
}
