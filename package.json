{
  "name": "ics-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "npm --prefix ./ics-backend start",
    "db:seed": "npm --prefix ./ics-backend run db:seed",
    "build": "npm --prefix ./ics-backend install"
  }
}
